NEXT_PUBLIC_APP_NAME=tpl
NEXT_PUBLIC_API_URL=/api
DEPLOYMENT_ENV=production
AUTH_APP=info
APP_ORIGIN=https://tpl.sunmoonai.com
WEB_BACKEND_INTERNAL_URL=http://tpl-web-backend:8000
DEPLOYMENT_ID=replace-with-release-id
REFERENCE_UI_ENABLED=false

# This tracked file is a non-secret deployment contract only. Kubernetes
# supplies environment-specific values. Casdoor, Redis, database and service
# credentials belong exclusively to the paired Nest Web Backend Secret.
